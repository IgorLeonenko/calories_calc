%h1 Домашняя страница

.row
  .col-md-12
    %h3 Нутриенты
    %table.table
      %thead
        %th Название
        %th Калорий в грамме
        %th Процентов в пище
      %tbody
        - @nutrients.each do |n|
          %tr.success
            %td
              = n.name
            %td
              = n.cal_per_gram
              калл на грамм
            %td
              = n.percents
              \%

  .col-md-6
    %h3 Рацион для веса
    %table.table.table-hover
      %thead
        %th Вес
        %th Действия
      %tbody
      - @weights.each do |w|
        %tr
          %td
            = w.my_weight
            кг
          %td
            = link_to "Посмотреть рацион", weight_path(w), class: "btn btn-info btn-xs"
            = link_to "X", weight_path(w), method: :delete, data: {confirm: "Точно?"},
              class: "btn btn-danger btn-xs"
  .col-md-6
